# Server Configuration
PORT=5000
NODE_ENV=development

# Database Connection (SQLite for local development)
DATABASE_URL="file:./dev.db"

# Redis Connection (optional - if not available, caching will be skipped)
REDIS_URL="redis://localhost:6379"

# JWT Secrets (Change these for prod)
JWT_SECRET="dev_secret_key_change_me"
JWT_REFRESH_SECRET="dev_refresh_secret_key_change_me"

# Client URL (for CORS)
CLIENT_URL="http://localhost:5173"
